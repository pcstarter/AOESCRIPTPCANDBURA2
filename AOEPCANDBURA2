-- ui libary
local library = loadstring(game:HttpGet(('https://raw.githubusercontent.com/AikaV3rm/UiLib/master/Lib.lua')))()
local w = library:CreateWindow('AOE') -- Creates the window
local b = w:CreateFolder('Buttons') -- Creates the folder(U will put here your buttons,etc)
local c = w:CreateFolder('Toggles') -- Creates the folder(U will put here your buttons,etc)
local q = w:CreateFolder('Boxes') -- Creates the folder(U will put here your buttons,etc)

local autoof = false
local autosell = false
local autofat = false
local quantity = 0
local item = 'nothinglol'

local mt = getrawmetatable(game)
    local old = mt.__namecall
    local protect = newcclosure or protect_function

    if not protect then
        print('Your exploit does not support protection against stack trace errors, resulting to fallback function')
        protect = function(f) return f end
    end

    setreadonly(mt, false)
    mt.__namecall = protect(function(self, ...)
        local method = getnamecallmethod()
        if method == 'Kick' then
            wait(9e9)
            return
        end
        return old(self, ...)
    end)
    hookfunction(game:GetService('Players').LocalPlayer.Kick,protect(function() wait(9e9) end))

    print('bypassed anti cheat!')



local message = 'Made by 120% pcstarter#8787 and BuraBURA14#9452'


local t = getnilinstances()
local interface 

for i,v in pairs(t) do 
    if v.ClassName == 'ModuleScript' and v.Name == 'Interface' then
        interface = require(v)
      end
end

local inventory 

for i,v in pairs(t) do 
    if v.ClassName == 'ModuleScript' and v.Name == 'Inventory' then
       inventory = require(v)
    end
end

local Integrity 

for i,v in pairs(t) do 
    if v.ClassName == 'ModuleScript' and v.Name == 'Integrity' then
        Integrity = require(v)
    end
end

interface:newHint(message)

b:Button('inf yield admin',function()
    loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
end)

b:Button('Revive lol',function()
game.Players.LocalPlayer.Status.Downed.Value = false 
end)

b:Button('max stats',function()

        -- Food
        Integrity.hunger =5000
        -- Warmth
        Integrity.warmth = 5000
        message = 'you now have max  hunger and warmth'
        interface:newHint(message)
end)

b:Button('Discord server',function()
setclipboard('discord.gg/bcT9w8r')
message = 'copied discord invite to your clipboard'
interface:newHint(message)
end)

b:Button('Buy item',function()

local args = {
    [1] = "IlllIIIIlII",
    [3] = "purchaseItem",
    [4] = "militiaSupplies",
    [5] = item,
    [6] = tonumber(quantity)
}

     for _,v in pairs(game:GetService("ReplicatedStorage").Resources.Remotes:GetChildren()) do
    if v:IsA('RemoteFunction') then
        if v.Name~='ServerRequestRemote' then
       v:InvokeServer(unpack(args))
        message = 'Sucesfully phurcased '..item
       interface:newHint(message)
        end
    end
end
end)

b:Button('no overencoumber',function()
      inventory.full = false
      message = 'you are no longer overencoumbered'
     interface:newHint(message)
end)

b:Button('sell  items',function()
    
for _,items in pairs(game.Players.LocalPlayer.Status.Items:GetChildren()) do
if items.Name~= 'Flintlock ball' then
if items.Name~= 'Bandage' then

local sellnow = {
    [1] = "IlllIIIIlII",
    [3] = "sellItem",
    [4] = "generalStore1",
    [5] = items.Name,
    [6] = 2
}

     for _,v in pairs(game:GetService("ReplicatedStorage").Resources.Remotes:GetChildren()) do
    if v:IsA('RemoteFunction') then
        if v.Name~='ServerRequestRemote' then
       v:InvokeServer(unpack(sellnow))
        end
    end
end
end
end
end
end)

q:Box('item to buy','string',function(name) -- "number" or "string"
        item = name
end)

q:Box('How much','number',function(itemvalue) -- "number" or "string"
   quantity = itemvalue 
end)

q:Box('Bonus warmth','number',function(value) -- "number" or "string"
inventory.warmthBonus = value
message = 'Sucesfully Changed warmth to : '..value
interface:newHint(message)
end)

-- toggles
c:Toggle('kill all',function(bool)
    shared.toggle = bool
    autoof = bool
end)



c:Toggle('anti overencoumbered',function(bool)
    shared.toggle = bool
    autofat = bool
end)

while wait() do
if autofat == true then
inventory.full = false
end
end
